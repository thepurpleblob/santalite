{{! A boolean variable 'haserrors' and an array containing the error strings }}

{{# haserrors }}
    <ul class="formerrors alert alert-danger">
        {{# errors }}
            <li>{{{ . }}}</li>
        {{/ errors }}
    </ul>
{{/ haserrors }}